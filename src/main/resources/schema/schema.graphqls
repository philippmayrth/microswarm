type Query {
    me: User
    user(id: String!): User
    device(id: String!): Device
}


type User {
    id: String!
    email: String!
    # TODO: Add devices
    # devices: [Device!]!
}

type Device {
    id: String!
}
